deten el cluster actual, luego modifica el config para crear un nuevo cluster, 1 control-plane y 5 worker...... 3 de esos worker veran la gpu y 1 worker que solo vea la cpu, con su etiqueta alusiva (GPU y CPU) a cada worker, igualmente en el config crea una etiqueta a cada worker para poder diferenciarlos a cada uno (ejemplo: worker1, worker2, etc), de forma que cada worker tenga 2 etiquetas......... adicional crea una carpeta llamada shared-workers (usa mkdir) y haz que todos los worker puedan ver esa carpeta......., no valides que ve la gpu con docker exec, sino que monta un servicio, por ejemplo: crea un pop de uso con la imagen nvidia/cuda:12.2.0-base-ubuntu22.04 y el comando nvidia-smi, mandalo a cada worker de forma controlada usando el label worker1, worker2, etc...... luego mira el log, si funciono el comando (nvidia-smi), con el servicio montado, es que la gpu se ve en el worker, sino es que funciona con cpu...... crea archivos de prueba, cada uno para enviar a un worker concreto........... luego crea un nuevo archivo de prueba que envie a cualquier worker que tenga la etiqueta GPU y otro archivo de prueba que envie a un worker con la etiqueta CPU, finalmente dame nuevamente la tabla de resultados con las 5 columnas que has definido (pon tambien la tabla en un worker_results.md), las columnas son: worker_name, labels, available_gpu, result_by_pop, notes