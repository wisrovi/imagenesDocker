mira todos los cambios de git que ha tenido esta carpeta, necesito que por cada archivo, archivo dentro de una carpeta, y cada archivo dentro de cualqueir subcarpeta, todo archivo debe ser mapeado en un commit diferente, no puede haber una carpeta en el git add, pero todos los archivos deben ser mapeados en un git add, con su propio git commit, texto en ingles perfecto, explicando que hace el archivo, el commit debe iniciar o con [FEATURE], [DOC], [REFACTOR], [FIX], , etcc....... un add se considera un FEATURE, crea un script llamado mapper_files.sh con los comandos que hacen el git add y el git commit a cada archivo william.rodriguez