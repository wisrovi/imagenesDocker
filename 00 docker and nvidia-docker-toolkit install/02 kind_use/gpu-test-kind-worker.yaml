apiVersion: v1
kind: Pod
metadata:
  name: gpu-test-kind-worker
spec:
  nodeName: kind-worker
  restartPolicy: Never
  containers:
  - name: gpu-container
    image: nvidia/cuda:12.2.0-base-ubuntu22.04
    command: ["sh", "-c", "ls /dev | grep nvidia || echo 'No GPU'"]