mira este foler, entiendelo, luego haz un make up SCALE=5, seguidamente se eecuta un python que crea un csv con las ips de cada worker (usando ssh para acceder a cada uno), luego usando el mismo ssh a cada worker haz dentro de cada uno un docker ps -a, y valida que el resultado se que todos estan running (pero nunca stop o similar), incluyendo el NVIDIA (nota, mi ordenador tiene GPU y se la comparto a cada worker para que cada uno la comprarta en su contenedor llamado NVIDIA), si algo falla corrigelo y valida, corrige y valida hasta que todo funcione (ssh, contenedores dentro de los worker)